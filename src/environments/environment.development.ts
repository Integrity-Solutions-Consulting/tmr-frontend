export const environment = {
  production: false,
  // Esta es la URL base de tu backend .NET (sin el /scalar)
  // Si tus endpoints tienen el prefijo /api (ej: /api/actividades), puedes poner 'http://localhost:5091/api'
  apiUrl: 'http://localhost:5091/api'
};
